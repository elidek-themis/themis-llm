# @package: _global_

task: "${oc.select: task, ???}"
model: "${oc.select: model, ???}"
type: multiple_choice
eval_kwargs:
  apply_chat_template: "${oc.select: apply_chat_template}"
  limit: "${oc.select: limit}"
  bootstrap_iters: "${oc.select: bootstrap_iters, 0}"
  random_seed: "${oc.select: seed}"
  numpy_random_seed: "${oc.select: seed}"
  torch_random_seed: "${oc.select: seed}"
  fewshot_random_seed: "${oc.select: seed}"
