defaults:
  - hydra: stores
  - interface: hf
  - evaluation: lm_eval_cli
  - models: instruct
  - _self_

model: ${models.${model_name}}
tasks: crows_pairs_completion_it_gen
apply_chat_template: ${models.apply_chat_template}
seed: 2025
