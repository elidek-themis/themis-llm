defaults:
  - hydra: model_sweep
  - interface: hf
  - evaluation: lm_eval_cli
  - models: instruct
  - _self_

model: ${models.${model_name}}
tasks: wino_bias_instruct
apply_chat_template: ${models.apply_chat_template}
seed: 2025
